<h1>Meetups</h1>

<ul>
  <% @meetups.each do |meetup| %>
  <li>
    <a href="meetups/<%= meetup.id %>"><%= meetup.name %> Meetup at <%= meetup.location %></a>
  </li>
  <% end %>
</ul>

<button>Add New Meetup</button>
